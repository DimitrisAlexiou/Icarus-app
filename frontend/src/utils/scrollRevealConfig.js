import ScrollReveal from 'scrollreveal';

const sr = ScrollReveal({
	origin: 'bottom',
	distance: '20px',
	duration: 2000,
	delay: 1000,
	easing: 'cubic-bezier(0.5, 0, 0, 1)',
	reset: false,
});

export default sr;
